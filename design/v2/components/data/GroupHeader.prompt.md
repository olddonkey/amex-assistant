List/group section header: label + optional badge + right-aligned summary. Cadence (月/季/半年/年) lives HERE, never as per-row chips. Amber badge only when period remaining < 25%.
```jsx
<GroupHeader label="每月" badge="还剩 19 天" summary="4 项 · $138.17 待用" />
```
