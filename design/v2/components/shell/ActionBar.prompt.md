Sticky bottom bar. Idle = grey hint; selected = count + PRIMARY button. Copy must never describe serial/queue/delay mechanics (严禁 串行/排队/延迟), and must not overclaim "all requests at once" either — v1.3 calibrated wording: 已选 {offers} 个 offer · 共 {adds} 次添加. Same-offer simultaneity is stated in the confirm dialog, not the bar.
```jsx
<ActionBar selectedCount={2} requestCount={16} />
```
