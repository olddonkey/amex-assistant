The ONE lightweight dropdown trigger at the top-right of a list view (grouping/sorting: 按到期/按卡/按类目). Never render a second segmented pill for this.
```jsx
<TextDropdown label="按到期" icon="list" />
```
Rule: dead controls are forbidden — only render it if the menu actually works.
