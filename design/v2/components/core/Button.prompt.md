Action button; primary is bright Amex blue for submit-type actions (e.g. 加到所选卡).
```jsx
<Button variant="primary">加到所选卡</Button>
<Button variant="secondary">返回列表</Button>
```
Never use navy for buttons — navy is reserved for filter/view state (双蓝规则).
