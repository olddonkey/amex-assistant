Small square-ish chip encoding a per-card STATE inline (宽模式核心构件). selected uses bright blue — it is a submit-type selection, never navy.
```jsx
<StatusChip label="⋯31004" art="silver" />
<StatusChip label="⋯21007 ✓ $5" art="gold" state="success" />
<StatusChip label="⋯21001" art="gold" state="selected" onClick={toggle} />
```
